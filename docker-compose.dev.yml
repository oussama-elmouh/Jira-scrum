services:
  app:
    build: .
    container_name: jira-scrum-dev
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: development
      NEXTAUTH_URL: http://localhost:3001
      NEXTAUTH_SECRET: dev-secret
      DATABASE_URL: file:./prisma/dev.db
